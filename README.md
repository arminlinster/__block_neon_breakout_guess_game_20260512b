# Neon Breakout

A high-performance neon-styled Breakout game built with React, TypeScript, and HTML5 Canvas.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
